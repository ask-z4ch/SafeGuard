# Unity WebGL Build Placeholder

Copy your exported Unity WebGL files (`index.html`, `Build/`, `TemplateData/`, etc.) into this `training-game/` directory. The React dashboard loads the module from `/training-game/index.html`.
